<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.xiaofangfang.butterknitedemo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        tools:replace="android:appComponentFactory"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:appComponentFactory="fangzhiyue"
        android:theme="@style/AppTheme">
        <activity android:name=".butter.ButterKniferActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".mvp.simple1.ui.Simple1"></activity>
        <activity android:name=".mvp.simple2.ui.Simple2"></activity>
        <activity android:name=".mvp.Simple4.ui.Simple4"></activity>
        <activity android:name=".mvp.simple5.ui.Simple5"></activity>
        <activity android:name=".mvp.simple6.Simple6"></activity>
        <activity android:name=".mvp.simple3.ui.Simple3">


        </activity>

    </application>

</manifest>